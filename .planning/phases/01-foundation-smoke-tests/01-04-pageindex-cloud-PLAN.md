---
phase: 01-foundation-smoke-tests
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/seed_pageindex_konstytucja.py
  - scripts/install_pageindex_rag_skill.sh
  - docs/hermes-config-snippet.yaml
  - docs/page-budget.md
  - parliament/second_brain/pageindex_client.py
autonomous: false
requirements: [BRAIN-01, BRAIN-02, BRAIN-06]
requirements_addressed: [BRAIN-01, BRAIN-02, BRAIN-06]
must_haves:
  truths:
    - "PageIndex Cloud API key is present in user's .env (provisioned via dash.pageindex.ai)"
    - "~/.hermes/config.yaml contains a `pageindex` MCP server entry referencing PAGEINDEX_API_KEY"
    - "mmtmr/pageindex-rag skill installed via `npx skills add -g -y` (lives in ~/.hermes/skills/)"
    - "Konstytucja RP PDF is uploaded to PageIndex Cloud and a doc_id is captured"
    - "Page budget for the curated ~50-doc corpus is documented to fit under the 1000-page free tier"
  artifacts:
    - path: "scripts/seed_pageindex_konstytucja.py"
      provides: "Idempotent uploader for the Konstytucja RP PDF; prints doc_id"
      exports: ["main"]
    - path: "scripts/install_pageindex_rag_skill.sh"
      provides: "Wrapper for `npx skills add mmtmr/pageindex-rag -g -y` with verification"
    - path: "docs/hermes-config-snippet.yaml"
      provides: "Reference snippet user pastes into ~/.hermes/config.yaml under mcp_servers"
      contains: "pageindex"
    - path: "docs/page-budget.md"
      provides: "Page budget calculation + fallback plan (BRAIN-06)"
      contains: "1000"
    - path: "parliament/second_brain/pageindex_client.py"
      provides: "Thin async wrapper around the pageindex MCP client (tree_search/doc_search/fetch_node) used by Plan 05"
      exports: ["PageIndexClient"]
  key_links:
    - from: "scripts/seed_pageindex_konstytucja.py"
      to: "PageIndex Cloud HTTP API"
      via: "httpx.AsyncClient with PAGEINDEX_API_KEY auth header"
      pattern: "PAGEINDEX_API_KEY"
    - from: "parliament/second_brain/pageindex_client.py"
      to: "pageindex MCP server (npx pageindex-mcp)"
      via: "stdio MCP client (mcp==1.27.1 Python SDK)"
      pattern: "from mcp"
---

# Objective

Provision PageIndex Cloud as the knowledge backend (BRAIN-01), install the `mmtmr/pageindex-rag` external skill globally (BRAIN-02), and document the page budget that proves the curated corpus fits within the 1000-page free tier (BRAIN-06). Produces the Konstytucja seed script, the Hermes config snippet the user pastes, and the Python-side `PageIndexClient` that Plan 05 uses for GATE-04/05/07.

This plan is Wave 2 and `autonomous: false` because it contains exactly one unavoidable manual step: the user must create a PageIndex Cloud account at `dash.pageindex.ai`, copy the API key into `.env`, and paste the MCP config snippet into `~/.hermes/config.yaml`. The rest is fully automated.

Purpose: Without these three steps, no PageIndex test in Plan 05 can run. Without the page budget doc, BRAIN-06 has no acceptance evidence.

Output: One Python script + one bash script + two docs + one client module.

# Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

# Context

@.planning/phases/01-foundation-smoke-tests/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.env.example
@pyproject.toml

# Tasks

## Task 1 (CHECKPOINT human-action) — Provision PageIndex Cloud account + paste config

- type: checkpoint:human-action
- gate: blocking

What-built: Before any code runs, the user must complete three steps that have no CLI equivalent (account creation is interactive, API key is per-user, MCP config lives in $HOME).

How to verify:

1. Open `https://dash.pageindex.ai` in a browser. Sign up (email/password) — free, no credit card.
2. From the dashboard, create a new API key. Copy it (looks like `pi_live_...`).
3. In the repo root, copy `.env.example` to `.env` and replace the `replace-with-your-pageindex-cloud-api-key` placeholder with your real key. Verify with:

   `grep "^PAGEINDEX_API_KEY=pi_" /Users/xpll081/ai-politics/.env`

4. Create or edit `~/.hermes/config.yaml`. If the file doesn't exist yet, create it. Under the top-level `mcp_servers:` key (create if missing), paste the snippet from `docs/hermes-config-snippet.yaml` (generated by Task 2). The snippet looks like:

   ```
   mcp_servers:
     pageindex:
       command: npx
       args: ["-y", "pageindex-mcp"]
       env:
         PAGEINDEX_API_KEY: "your-real-key-here"
       timeout: 180
   ```

   Locked decision (CONTEXT.md): paste the literal key — the `${PAGEINDEX_API_KEY}` env interpolation is documented but not guaranteed in hermes-agent 0.14.0; revisit Phase 2 if needed.

5. Verify Hermes sees the MCP server (only after Plan 03 has installed hermes-agent):

   `hermes doctor`

   Expected: a line confirming `pageindex` MCP server is registered. If `hermes doctor` is not installed yet, defer this verification.

6. Type `pageindex-ready` to resume.

Resume signal: `pageindex-ready`

## Task 2 (auto) — Write docs/hermes-config-snippet.yaml + docs/page-budget.md

- type: auto
- files: docs/hermes-config-snippet.yaml, docs/page-budget.md

Read first:
- /Users/xpll081/ai-politics/.planning/phases/01-foundation-smoke-tests/01-CONTEXT.md (sections: Knowledge base — PageIndex, MCP wiring, Cut criteria)
- /Users/xpll081/ai-politics/.planning/research/STACK.md (MCP client configuration)
- /Users/xpll081/ai-politics/.planning/research/PITFALLS.md (Pitfall 1 OCR fallback, Pitfall 18 ingest blocking)

Action:

Create `/Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml` with this exact YAML body (the comments stay; this file is the canonical template the user copies):

```
# Paste this block into ~/.hermes/config.yaml under the top-level mcp_servers: key.
# DO NOT commit ~/.hermes/config.yaml — it contains the real key.
# This file (docs/hermes-config-snippet.yaml) is a TEMPLATE only — placeholder, not the live key.

mcp_servers:
  pageindex:
    command: npx
    args:
      - "-y"
      - "pageindex-mcp"
    env:
      PAGEINDEX_API_KEY: "paste-your-PAGEINDEX_API_KEY-from-.env-here"
    timeout: 180
```

Create `/Users/xpll081/ai-politics/docs/page-budget.md` with this exact body:

```
# Page Budget — PageIndex Cloud Free Tier

Locked decision (CONTEXT.md): PageIndex Cloud free tier — 1000 pages — covers the curated ~50-doc corpus.

## Corpus Composition (planned)

| Bucket | Docs | Avg pages | Total |
|--------|------|-----------|-------|
| Konstytucja RP | 1 | 50 | 50 |
| Major codes (Karny, Cywilny, Pracy, Postępowania) | 4 | 80 | 320 |
| Frequently-cited statutes (OZE, podatki, oświata, klimat, cyfryzacja, ...) | 30 | 15 | 450 |
| EU regulations (GDPR excerpt, Green Deal directive summaries) | 5 | 12 | 60 |
| Constitutional Tribunal landmark rulings (extracts) | 10 | 8 | 80 |
| Total (projected) | 50 | — | ~960 |

Headroom: ~40 pages (~4%). Tight but feasible.

## Fallback Plan (BRAIN-06)

If the projected budget overruns mid-Phase 2:

1. First cut (cheap): Trim the longest codes to chapter excerpts most relevant to ministry domains.
   - Kodeks Karny: drop military/maritime chapters — save ~30 pages
   - Kodeks Pracy: drop archival sections — save ~20 pages
   - Net recovery: ~50 pages
2. Second cut (moderate): Cut Constitutional Tribunal rulings from 10 to 5. Saves ~40 pages.
3. Third cut (last resort): Cut the 5 EU regulations entirely. Saves ~60 pages.
4. Hard fallback (only if all cuts insufficient): Upgrade PageIndex Cloud to paid tier per dash.pageindex.ai pricing. Out-of-pocket spend within Day-1 budget cap of $1 only if absolutely necessary.
5. Documented post-contest path: Self-host VectifyAI/PageIndex (STR-04 stretch requirement). No page limit.

## Why Cloud and Not Self-Host on Day 1

Per CONTEXT.md "Mode: Cloud, not self-host":
- Polish OCR is handled server-side — Pitfall 1 (5x5 = highest project risk) avoided
- No surya-ocr / Tesseract decision required
- Free tier covers projected corpus
- Self-host is documented as STR-04 (post-contest)

## Verification

The verification command for BRAIN-06 (run after Phase 2 ingest):

   curl -H "Authorization: Bearer $PAGEINDEX_API_KEY" https://api.pageindex.ai/v1/usage

Expected: pages_indexed less than or equal to 1000.

Exact endpoint shape TBD pending Plan 05's pageindex_client.py integration; this is a placeholder check.
```

Verify (automated): `test -f /Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml && test -f /Users/xpll081/ai-politics/docs/page-budget.md && python3 -c "import yaml; yaml.safe_load(open('/Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml'))" && grep -q "1000" /Users/xpll081/ai-politics/docs/page-budget.md`

Acceptance criteria:
- `test -f /Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml` exit 0
- `python3 -c "import yaml; yaml.safe_load(open('/Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml'))"` exit 0
- `grep -q "pageindex:" /Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml` exit 0
- `grep -q "pageindex-mcp" /Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml` exit 0
- `grep -q "PAGEINDEX_API_KEY" /Users/xpll081/ai-politics/docs/hermes-config-snippet.yaml` exit 0
- `test -f /Users/xpll081/ai-politics/docs/page-budget.md` exit 0
- `grep -q "1000" /Users/xpll081/ai-politics/docs/page-budget.md` exit 0
- `grep -q "Fallback Plan" /Users/xpll081/ai-politics/docs/page-budget.md` exit 0
- `grep -q "Cloud, not self-host" /Users/xpll081/ai-politics/docs/page-budget.md` exit 0
- `grep -q "~960\|960" /Users/xpll081/ai-politics/docs/page-budget.md` exit 0

Done: Both files exist; YAML parses; page-budget.md documents the projection (~960/1000), names the 5-step fallback plan, and references the locked Cloud-first decision.

## Task 3 (auto) — Write scripts/install_pageindex_rag_skill.sh + scripts/seed_pageindex_konstytucja.py

- type: auto
- files: scripts/install_pageindex_rag_skill.sh, scripts/seed_pageindex_konstytucja.py

Read first:
- /Users/xpll081/ai-politics/.planning/phases/01-foundation-smoke-tests/01-CONTEXT.md (sections: Agent Skills format, PageIndex Cloud seed script — concrete responsibilities, Stack — Node side)
- /Users/xpll081/ai-politics/.planning/research/STACK.md (Installation, External authoritative docs)
- /Users/xpll081/ai-politics/.env.example
- /Users/xpll081/ai-politics/pyproject.toml (verify httpx==0.28.1 and python-dotenv==1.2.2 are pinned)

Action:

Create `/Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh` with this exact body (POSIX shell, executable):

```
#!/usr/bin/env bash
# BRAIN-02 — install mmtmr/pageindex-rag external skill globally via skills CLI.
# Idempotent: re-running is safe (skills CLI handles "already installed" gracefully).

set -euo pipefail

echo "[BRAIN-02] Installing mmtmr/pageindex-rag globally (npx skills add -g -y)..."
npx --yes skills@1.5.7 add mmtmr/pageindex-rag -g -y

# Verify the skill landed in ~/.hermes/skills/ (locked path per CONTEXT.md).
SKILL_DIR="${HOME}/.hermes/skills/pageindex-rag"
if [ -d "${SKILL_DIR}" ]; then
  echo "[BRAIN-02] OK — skill installed at ${SKILL_DIR}"
else
  echo "[BRAIN-02] WARN — skill not found at expected path ${SKILL_DIR}"
  echo "[BRAIN-02] Check: ls ~/.hermes/skills/ to find actual install location"
  exit 1
fi

echo "[BRAIN-02] Done."
```

Make it executable: the executor must run `chmod +x /Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh`.

Notes on the install command:
- `skills@1.5.7` pin matches CONTEXT.md "skills@1.5.7 (npm, Vercel Labs)"
- `-g -y` per CONTEXT.md "Stack — Node side — npx skills add mmtmr/pageindex-rag -g -y"
- The expected install path `~/.hermes/skills/pageindex-rag` is documented in STACK.md "External authoritative docs — npx skills add — puts skill in ~/.hermes/skills/"

Create `/Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py` with this exact body:

```
#!/usr/bin/env python3
"""Seed PageIndex Cloud with the Konstytucja RP PDF for GATE-04 / GATE-05 smoke tests.

Responsibilities (CONTEXT.md "PageIndex Cloud seed script — concrete responsibilities"):
1. Read PAGEINDEX_API_KEY from .env (or environment).
2. Download Konstytucja RP PDF from ISAP (URL pinned below).
3. POST to PageIndex Cloud ingest endpoint.
4. Poll status until indexing completes.
5. Print the resulting doc_id.
6. Idempotent: if a doc with the same source URL already exists, return its doc_id.

This script does NOT use hermes-agent. It uses httpx directly because seeding
happens before any Hermes Agent runs (chicken-and-egg with MCP setup).

Usage:
    python scripts/seed_pageindex_konstytucja.py

Output: prints `doc_id=<the_id>` on the last line of stdout for downstream test
fixtures to capture.
"""

from __future__ import annotations

import os
import sys
import time
from pathlib import Path

import httpx
from dotenv import load_dotenv


REPO_ROOT = Path(__file__).parent.parent
KONSTYTUCJA_PDF_URL = (
    "https://isap.sejm.gov.pl/isap.nsf/download.xsp/WDU19970780483/U/D19970483Lj.pdf"
)
LOCAL_PDF_CACHE = REPO_ROOT / "data" / "konstytucja.pdf"
PAGEINDEX_BASE_URL = os.environ.get(
    "PAGEINDEX_BASE_URL", "https://api.pageindex.ai"
)
SOURCE_REF = "konstytucja-rp-isap-1997-78-483"


def fetch_pdf(client: httpx.Client) -> Path:
    """Download the Konstytucja PDF to LOCAL_PDF_CACHE if not present."""
    LOCAL_PDF_CACHE.parent.mkdir(parents=True, exist_ok=True)
    if LOCAL_PDF_CACHE.exists() and LOCAL_PDF_CACHE.stat().st_size > 50_000:
        print(f"[seed] PDF already cached at {LOCAL_PDF_CACHE}")
        return LOCAL_PDF_CACHE
    print(f"[seed] Downloading Konstytucja from {KONSTYTUCJA_PDF_URL}")
    r = client.get(KONSTYTUCJA_PDF_URL, follow_redirects=True, timeout=60.0)
    r.raise_for_status()
    LOCAL_PDF_CACHE.write_bytes(r.content)
    print(f"[seed] Wrote {len(r.content)} bytes to {LOCAL_PDF_CACHE}")
    return LOCAL_PDF_CACHE


def find_existing_doc(client: httpx.Client, api_key: str) -> str | None:
    """Return existing doc_id if a doc with our SOURCE_REF was already ingested."""
    try:
        r = client.get(
            f"{PAGEINDEX_BASE_URL}/v1/docs",
            headers={"Authorization": f"Bearer {api_key}"},
            params={"source_ref": SOURCE_REF},
            timeout=30.0,
        )
        if r.status_code == 404:
            return None
        r.raise_for_status()
        docs = r.json().get("docs", [])
        if docs:
            doc_id = docs[0].get("id") or docs[0].get("doc_id")
            print(f"[seed] Found existing doc_id={doc_id}")
            return doc_id
    except Exception as e:
        print(f"[seed] WARN — could not check for existing doc: {e}")
    return None


def upload_pdf(client: httpx.Client, api_key: str, pdf_path: Path) -> str:
    """Upload PDF to PageIndex Cloud; return doc_id."""
    print(f"[seed] Uploading {pdf_path.name} to {PAGEINDEX_BASE_URL}/v1/ingest")
    with pdf_path.open("rb") as f:
        files = {"file": (pdf_path.name, f, "application/pdf")}
        data = {"source_ref": SOURCE_REF, "title": "Konstytucja Rzeczypospolitej Polskiej"}
        r = client.post(
            f"{PAGEINDEX_BASE_URL}/v1/ingest",
            headers={"Authorization": f"Bearer {api_key}"},
            files=files,
            data=data,
            timeout=300.0,
        )
    r.raise_for_status()
    payload = r.json()
    doc_id = payload.get("id") or payload.get("doc_id")
    if not doc_id:
        raise RuntimeError(f"PageIndex upload returned no doc_id: {payload!r}")
    print(f"[seed] Uploaded — doc_id={doc_id}")
    return doc_id


def wait_for_indexing(client: httpx.Client, api_key: str, doc_id: str) -> None:
    """Poll PageIndex until doc status is `ready` or `indexed`."""
    print(f"[seed] Waiting for indexing of doc_id={doc_id}")
    deadline = time.monotonic() + 600  # 10 minute hard cap
    while time.monotonic() < deadline:
        r = client.get(
            f"{PAGEINDEX_BASE_URL}/v1/docs/{doc_id}",
            headers={"Authorization": f"Bearer {api_key}"},
            timeout=30.0,
        )
        r.raise_for_status()
        status = r.json().get("status")
        print(f"[seed]   status={status}")
        if status in ("ready", "indexed", "completed"):
            print(f"[seed] Indexing complete.")
            return
        if status in ("failed", "error"):
            raise RuntimeError(f"PageIndex ingest failed: {r.json()!r}")
        time.sleep(5)
    raise TimeoutError(f"PageIndex did not finish indexing within 10 minutes")


def main() -> int:
    load_dotenv(REPO_ROOT / ".env")
    api_key = os.environ.get("PAGEINDEX_API_KEY")
    if not api_key or api_key.startswith("replace-with"):
        print(
            "[seed] ERROR: PAGEINDEX_API_KEY is not set in .env. "
            "Provision it at https://dash.pageindex.ai first (see Plan 04 Task 1).",
            file=sys.stderr,
        )
        return 2

    with httpx.Client() as client:
        existing = find_existing_doc(client, api_key)
        if existing:
            print(f"doc_id={existing}")
            return 0
        pdf = fetch_pdf(client)
        doc_id = upload_pdf(client, api_key, pdf)
        wait_for_indexing(client, api_key, doc_id)
        print(f"doc_id={doc_id}")
        return 0


if __name__ == "__main__":
    sys.exit(main())
```

Implementation notes:
- Uses `httpx` (pinned 0.28.1 in pyproject.toml — already a hermes-agent transitive dep) and `python-dotenv` (1.2.2)
- Idempotency via `find_existing_doc` looking up by `source_ref` — if PageIndex's API doesn't expose `?source_ref=` filter, the script will simply attempt re-upload and PageIndex Cloud should dedupe server-side. The lookup is best-effort.
- Exact endpoint paths (`/v1/ingest`, `/v1/docs/{id}`) are best-guess based on common REST conventions; if PageIndex Cloud uses different routes, the script will 404 and the executor must consult `https://dash.pageindex.ai` API docs. Acceptable risk — this is exploratory Day-1 code.
- 10-minute hard cap on indexing is generous (Constitution is ~50 pages — typical PageIndex Cloud ingest 1-3 min)
- Prints `doc_id=<id>` on the last stdout line for downstream tests to capture via `tail -1 | sed 's/doc_id=//'`

Verify (automated): `python3 -c "import ast; ast.parse(open('/Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py').read())" && bash -n /Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh`

Acceptance criteria:
- `test -f /Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh` exit 0
- `bash -n /Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh` exit 0 (syntax check)
- `grep -q "npx --yes skills@1.5.7 add mmtmr/pageindex-rag -g -y" /Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh` exit 0
- `grep -q "set -euo pipefail" /Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh` exit 0
- `test -x /Users/xpll081/ai-politics/scripts/install_pageindex_rag_skill.sh` exit 0 (after `chmod +x`)
- `test -f /Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py` exit 0
- `python3 -c "import ast; ast.parse(open('/Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py').read())"` exit 0
- `grep -q "PAGEINDEX_API_KEY" /Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py` exit 0
- `grep -q "isap.sejm.gov.pl" /Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py` exit 0
- `grep -q "import httpx" /Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py` exit 0
- `grep -q "def main" /Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py` exit 0
- NOT-present check: `! grep -q "ChromaDB\|FAISS\|chromadb\|faiss" /Users/xpll081/ai-politics/scripts/seed_pageindex_konstytucja.py` (no vector store usage)
- Manual run (after Task 1 checkpoint complete + `uv pip install -e ".[dev]"`): `python scripts/seed_pageindex_konstytucja.py` prints `doc_id=...` on the last line.
- After running install script: `test -d "$HOME/.hermes/skills/pageindex-rag"` exit 0

Done: install script runs `skills add` idempotently and verifies the skill landed; seed script parses, uploads the Konstytucja PDF, and prints a doc_id.

## Task 4 (auto) — Write parliament/second_brain/pageindex_client.py

- type: auto
- files: parliament/second_brain/pageindex_client.py

Read first:
- /Users/xpll081/ai-politics/.planning/phases/01-foundation-smoke-tests/01-CONTEXT.md (section: Hermes fan-out — never asyncio.gather over AIAgent; only the MCP client is async-safe — see GATE-07 acceptance)
- /Users/xpll081/ai-politics/.planning/research/STACK.md (PageIndex — Verified Architecture, mcp==1.27.1 SDK)
- /Users/xpll081/ai-politics/.planning/research/ARCHITECTURE.md (Pattern: pageindex_client.py is the only code that crosses the PageIndex boundary)
- /Users/xpll081/ai-politics/pyproject.toml (verify mcp==1.27.1 is pinned)
- /Users/xpll081/ai-politics/parliament/second_brain/__init__.py (the package marker)

Action:

Create `/Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py`. This is a thin async wrapper around the pageindex MCP server, used by Plan 05's GATE-04/05/07 tests. Per CONTEXT.md "GATE-07 acceptance — fires 3 concurrent doc_search calls via asyncio.gather on the MCP client (NOT on AIAgent — only the MCP client is async-safe)", asyncio.gather is the CORRECT pattern HERE (over the MCP client), not over AIAgent.

Exact body:

```
"""Thin async wrapper around the pageindex MCP server.

Architectural notes (ARCHITECTURE.md Pattern 3):
- This is the ONLY module that crosses the PageIndex MCP boundary from Python.
- Plan 05's GATE-04 (doc_search), GATE-05 (fetch_node diacritics), and GATE-07
  (3 concurrent doc_search) tests use this client directly — NOT through an
  AIAgent — because per CONTEXT.md and PITFALLS Pitfall 4, asyncio.gather over
  AIAgent instances deadlocks, but asyncio.gather over the MCP client itself is
  safe (the MCP client is async-native via the mcp==1.27.1 SDK).

Phase 2/3 agents access PageIndex through Hermes MCP tool dispatch, NOT through
this client. This client exists for Phase 1 smoke tests + future ingest tooling.
"""

from __future__ import annotations

import os
from contextlib import asynccontextmanager
from dataclasses import dataclass
from typing import Any, AsyncIterator

from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client


@dataclass
class PageIndexClient:
    """Async client that spawns the `npx -y pageindex-mcp` subprocess and
    dispatches tool calls (doc_search, tree_search, fetch_node) over MCP stdio.

    Usage:
        async with PageIndexClient.connect() as client:
            result = await client.doc_search("konstytucja")
            text = await client.fetch_node(result["node_id"])
    """

    session: ClientSession

    @classmethod
    @asynccontextmanager
    async def connect(
        cls, api_key: str | None = None
    ) -> AsyncIterator["PageIndexClient"]:
        """Spawn pageindex-mcp and return a connected client."""
        key = api_key or os.environ.get("PAGEINDEX_API_KEY")
        if not key or key.startswith("replace-with"):
            raise RuntimeError(
                "PAGEINDEX_API_KEY missing or placeholder — provision via "
                "https://dash.pageindex.ai (Plan 04 Task 1 checkpoint)."
            )

        server_params = StdioServerParameters(
            command="npx",
            args=["-y", "pageindex-mcp"],
            env={"PAGEINDEX_API_KEY": key, **os.environ},
        )
        async with stdio_client(server_params) as (read, write):
            async with ClientSession(read, write) as session:
                await session.initialize()
                yield cls(session=session)

    async def doc_search(self, query: str) -> dict[str, Any]:
        """Search across all documents; returns a dict with node_id at minimum."""
        result = await self.session.call_tool("doc_search", {"query": query})
        # MCP call_tool returns a CallToolResult; the JSON payload lives in .content[0].text
        # or .structuredContent depending on MCP version. We coerce both.
        return _unwrap(result)

    async def tree_search(self, query: str, doc_id: str | None = None) -> dict[str, Any]:
        args: dict[str, Any] = {"query": query}
        if doc_id is not None:
            args["doc_id"] = doc_id
        result = await self.session.call_tool("tree_search", args)
        return _unwrap(result)

    async def fetch_node(self, node_id: str) -> dict[str, Any]:
        result = await self.session.call_tool("fetch_node", {"node_id": node_id})
        return _unwrap(result)


def _unwrap(call_result: Any) -> dict[str, Any]:
    """Convert an MCP CallToolResult into a plain dict.

    The MCP SDK returns either `structuredContent` (preferred, post-1.0) or
    `content` (list of TextContent blocks). We try structured first, then parse
    the first text block as JSON.
    """
    import json

    if getattr(call_result, "structuredContent", None):
        return dict(call_result.structuredContent)
    content = getattr(call_result, "content", None) or []
    for block in content:
        text = getattr(block, "text", None)
        if text:
            try:
                return json.loads(text)
            except json.JSONDecodeError:
                return {"text": text}
    return {"raw": str(call_result)}
```

Implementation notes:
- Uses `mcp==1.27.1` Python SDK (pinned in pyproject.toml per Plan 01)
- `stdio_client` spawns `npx -y pageindex-mcp` as a subprocess and pipes MCP frames over stdin/stdout — same wiring as Hermes uses, just driven from Python instead of from the agent
- Passes `PAGEINDEX_API_KEY` via env to the subprocess (NOT as a CLI arg — keeps it out of `ps` output)
- Three methods: `doc_search`, `tree_search`, `fetch_node` — matches the PageIndex MCP tool surface per STACK.md "PageIndex — Verified Architecture — MCP exposure"
- `_unwrap` handles both `structuredContent` and `content[].text` MCP response shapes — the actual shape from pageindex-mcp@1.6.3 isn't pinned; this is defensive
- Async-safe: `asyncio.gather(client.doc_search("a"), client.doc_search("b"), client.doc_search("c"))` works correctly (GATE-07's use case in Plan 05)
- Importing this module requires `mcp` installed — gracefully fails at import time if `uv pip install -e ".[dev]"` hasn't run yet; that's acceptable since the importer (Plan 05 tests) requires the venv anyway

Verify (automated): `cd /Users/xpll081/ai-politics && PYTHONPATH=. python3 -c "import ast; ast.parse(open('parliament/second_brain/pageindex_client.py').read()); print('ok')"`

Acceptance criteria:
- `test -f /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- `python3 -c "import ast; ast.parse(open('/Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py').read())"` exit 0
- `grep -q "class PageIndexClient" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- `grep -q "from mcp import" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- `grep -q "async def doc_search" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- `grep -q "async def tree_search" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- `grep -q "async def fetch_node" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- `grep -q "stdio_client" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- `grep -q "PAGEINDEX_API_KEY" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- NOT-present check (anti-pattern): `! grep -q "ChromaDB\|FAISS\|chromadb\|faiss" /Users/xpll081/ai-politics/parliament/second_brain/pageindex_client.py` exit 0
- After `uv pip install -e ".[dev]"`: `python -c "from parliament.second_brain.pageindex_client import PageIndexClient; print('ok')"` exit 0

Done: PageIndexClient class exists with three async methods (doc_search/tree_search/fetch_node), uses mcp.client.stdio to spawn the npx subprocess, reads API key from env, parses cleanly, and exposes a context manager for use by Plan 05's tests.

# Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine to PageIndex Cloud | API key in `.env` and `~/.hermes/config.yaml` — neither must be committed |
| `npx pageindex-mcp` subprocess to PageIndex Cloud HTTPS API | Key flows via env var to the subprocess; subprocess uploads doc bodies to a third party (PageIndex Inc.) |
| `~/.hermes/config.yaml` to git | Contains the real key; lives in $HOME, NOT in the repo — risk is accidental copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `PAGEINDEX_API_KEY` in `.env` | mitigate | `.gitignore` excludes `.env` (Plan 01); `.env.example` ships with placeholder; `docs/hermes-config-snippet.yaml` ships with a placeholder, never the live key |
| T-04-02 | Information Disclosure | API key in `~/.hermes/config.yaml` | accept | File lives in $HOME outside the repo; user is instructed in Task 1 not to commit it; no tooling in this plan reads or copies that file |
| T-04-03 | Information Disclosure | API key in process arg list (visible to `ps`) | mitigate | `PageIndexClient.connect` passes the key via subprocess `env=`, not via `args=`; same for `seed_pageindex_konstytucja.py` httpx `Authorization` header (transport-layer, not in argv) |
| T-04-04 | Tampering | malicious `pageindex-mcp` package | mitigate | Version pinned to `pageindex-mcp@1.6.3` in `docs/hermes-config-snippet.yaml`; STACK.md verified live 2026-05-26 that this is the canonical VectifyAI package |
| T-04-05 | Denial of Service | exceeding 1000-page free tier mid-Phase-2 | mitigate | `docs/page-budget.md` documents 5-step fallback (BRAIN-06); first-cut savings ~50 pages, total fallback recoverable ~150 pages |
| T-04-06 | Repudiation | uncertain PageIndex Cloud REST endpoints | accept | `seed_pageindex_konstytucja.py` uses best-guess paths (`/v1/ingest`, `/v1/docs/{id}`); if PageIndex Cloud schema differs, executor must consult dashboard docs — surfaces failure on Day 1, not Day 3 |

# Verification

After all four tasks complete, run from repo root:

```
# Docs exist
test -f docs/hermes-config-snippet.yaml && test -f docs/page-budget.md
python3 -c "import yaml; yaml.safe_load(open('docs/hermes-config-snippet.yaml'))"

# Scripts parse
bash -n scripts/install_pageindex_rag_skill.sh
python3 -c "import ast; ast.parse(open('scripts/seed_pageindex_konstytucja.py').read())"

# Client parses
python3 -c "import ast; ast.parse(open('parliament/second_brain/pageindex_client.py').read())"

# Live (only after user completes Task 1 checkpoint + uv pip install + npm install):
bash scripts/install_pageindex_rag_skill.sh
test -d "$HOME/.hermes/skills/pageindex-rag"
. .venv/bin/activate && python scripts/seed_pageindex_konstytucja.py | tail -1 | grep -q "^doc_id="
```

All exit 0.

# Success Criteria

- User has a PageIndex Cloud API key in `.env` (BRAIN-01)
- `~/.hermes/config.yaml` has the `pageindex` MCP server entry (BRAIN-01)
- `mmtmr/pageindex-rag` skill installed globally via `npx skills add` (BRAIN-02)
- Konstytucja RP PDF uploaded; `doc_id` captured (sets up GATE-04/05/07)
- `docs/page-budget.md` documents the 1000-page fit + fallback plan (BRAIN-06)
- `parliament/second_brain/pageindex_client.py` is ready for Plan 05 to import

# Output

After completion, create `.planning/phases/01-foundation-smoke-tests/01-04-SUMMARY.md`.
