---
phase: quick-260528-ea6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - skills/web-research/SKILL.md
  - parliament/agent_factory.py
  - tests/test_web_search_agent.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The Hermes CLI (agent-cli) is verified present and a keyless web-search backend is configured"
    - "A web-research agent skill exists and validates against the Agent Skills spec"
    - "agent_factory can build a Hermes command that attaches the web toolset to any skill"
    - "Running the web-research agent on a query returns live web results without an API key"
  artifacts:
    - path: "skills/web-research/SKILL.md"
      provides: "Web search agent skill using the Hermes web toolset"
      contains: "name: web-research"
    - path: "parliament/agent_factory.py"
      provides: "build_hermes_cmd with optional toolsets parameter"
      contains: "toolsets"
    - path: "tests/test_web_search_agent.py"
      provides: "Unit test for toolset wiring + skill frontmatter validity"
  key_links:
    - from: "parliament/agent_factory.py"
      to: "hermes chat -t web"
      via: "argv list includes -t <toolsets>"
      pattern: "\\-t"
    - from: "skills/web-research/SKILL.md"
      to: "web_search tool"
      via: "skill instructs agent to call web_search"
      pattern: "web_search"
---

<objective>
Install/verify the Hermes Agent CLI ("agent-cli") and build a web-search-capable agent so
parliament agents can research current, non-corpus facts on the open web.

The Hermes CLI is already installed at `/Users/xpll081/.pyenv/shims/hermes` and ships a built-in
`web` toolset exposing `web_search` and `web_extract`. The missing pieces are: (1) a keyless
search backend (`ddgs` / DuckDuckGo — no API key, fits the $5 VPS budget), (2) a `web-research`
agent skill following the existing skill format, and (3) factory support for attaching the
`web` toolset to a Hermes subprocess so `session.py` can invoke it like any other agent.

Purpose: Give the parliament a live web-research capability that complements the PageIndex
legal corpus, using only keyless infrastructure so it works on a clean clone.
Output: Configured search backend, a validated `web-research` skill, factory toolset support, and a smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Verified during planning. Use directly — no exploration needed. -->

The Hermes CLI is on PATH (`hermes`, a pyenv shim). Verified invocation pattern (from parliament/agent_factory.py):
    hermes chat -s <skill> -q <query> -Q --accept-hooks --yolo

The `-t / --toolsets` flag attaches built-in toolsets. The relevant toolset is:
    "web": tools = ["web_search", "web_extract"]   # defined in installed toolsets.py

Web search backends (from installed tools/web_tools.py `_get_backend`):
    Allowed config values: parallel, firecrawl, tavily, exa, searxng, brave-free, ddgs
    `ddgs` = DuckDuckGo HTML scrape, NO API KEY required (keyless). Requires the `ddgs` PyPI package.
    Config keys (nested, set via `hermes config set web.search_backend ddgs`):
        web.backend          (shared fallback)
        web.search_backend   (per-capability override for search)

Current `~/.hermes/config.yaml` has `web.search_backend: ''` (empty) and toolsets: [hermes-cli].
The `ddgs` package is NOT yet installed (verified: `import ddgs` fails).

Existing factory (parliament/agent_factory.py):
    build_hermes_cmd(skill: str, query: str, *, tier: ModelTier = "orchestrator") -> list[str]
    Returns: ["hermes", "chat", "-s", skill, "-q", query, "-Q", "--accept-hooks", "--yolo"]
    _HERMES_BASE_FLAGS = ("-Q", "--accept-hooks", "--yolo")

Existing skill frontmatter shape (skills/ministry-finansow/SKILL.md):
    ---
    name: <lowercase-hyphen>
    description: <one sentence>
    license: MIT
    metadata:
      type: <ministry|orchestrator|...>
      model_tier: <orchestrator|ministry|party>
    ---

Validation command (per CLAUDE.md): `npx skills-ref@0.1.5 validate skills/web-research` must exit 0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install keyless web-search backend and wire Hermes config</name>
  <files>pyproject.toml, .env.example</files>
  <action>
Add the keyless DuckDuckGo search backend so the `web` toolset works with no API key.

1. In pyproject.toml `[project.dependencies]`, add `"ddgs"` (the PyPI package backing the
   Hermes `ddgs` backend — keyless DuckDuckGo). Keep alphabetical/logical grouping with other deps.
2. Install it into the active environment: `pip install ddgs`.
3. Configure Hermes to use it for search (nested keys, no editor — non-interactive):
     hermes config set web.backend ddgs
     hermes config set web.search_backend ddgs
4. In .env.example, append a short documented OPTIONAL block noting that web search uses keyless
   DuckDuckGo by default (no key needed), and that BRAVE_SEARCH_API_KEY or TAVILY_API_KEY may be
   set later to upgrade the backend. Do NOT add any required secret — keyless is the default.

Use `ddgs` (not a paid backend) because the project must run on a ~$5/month VPS and on a clean
clone with zero secrets. Do NOT add an API key requirement to .env (keep keyless as the default).
  </action>
  <verify>
    <automated>python -c "import ddgs; print('ddgs ok')" && python -c "import yaml; c=yaml.safe_load(open(__import__('os').path.expanduser('~/.hermes/config.yaml'))); assert c['web']['search_backend']=='ddgs', c['web']; print('backend ok')"</automated>
  </verify>
  <done>`import ddgs` succeeds, `web.search_backend == 'ddgs'` in ~/.hermes/config.yaml, pyproject lists ddgs, .env.example documents the keyless default.</done>
</task>

<task type="auto">
  <name>Task 2: Create the web-research agent skill</name>
  <files>skills/web-research/SKILL.md</files>
  <action>
Create a new Agent Skill at `skills/web-research/SKILL.md` following the EXACT frontmatter shape
used by existing skills (see ministry-finansow interface block).

Frontmatter (must validate against skills-ref):
    ---
    name: web-research
    description: Web research agent that searches the open web for current facts and statistics to support parliamentary debate.
    license: MIT
    metadata:
      type: research
      model_tier: ministry
    ---

Body must instruct the agent to:
- Use the `web_search` tool (and `web_extract` when a source page needs reading) to find current,
  non-corpus information (recent statistics, news, economic data) relevant to a bill topic.
- Output in English; quote foreign-language sources with (orig. ...) notation, mirroring the
  ministry convention.
- ALWAYS attach a source URL for every factual claim (the web analogue of PageIndex node citations).
- Stay stateless and non-partisan — it provides facts, not political positions (same stance as ministries).
- Include an `## Output Constraints` section prohibiting real MP names and hate speech, and requiring
  a source URL per claim, matching the project ethics requirements in CLAUDE.md.

Keep the file ASCII-clean in frontmatter (skills-ref rejects non-ASCII in frontmatter fields).
  </action>
  <verify>
    <automated>npx -y skills-ref@0.1.5 validate skills/web-research && grep -q "web_search" skills/web-research/SKILL.md</automated>
  </verify>
  <done>`npx skills-ref@0.1.5 validate skills/web-research` exits 0; SKILL.md references `web_search` and has an Output Constraints section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add toolset support to agent_factory and smoke-test the web agent</name>
  <files>parliament/agent_factory.py, tests/test_web_search_agent.py</files>
  <behavior>
    - build_hermes_cmd with no toolsets returns the existing argv unchanged (backward compatible: no `-t` flag).
    - build_hermes_cmd(..., toolsets=["web"]) inserts `-t web` into the argv (after the query, before/among base flags).
    - build_hermes_cmd(..., toolsets=["web","search"]) joins multiple toolsets as a comma-separated value `web,search` (Hermes -t accepts comma-separated names).
    - Invalid toolset entries (empty string, containing whitespace) raise ValueError.
    - The web-research SKILL.md frontmatter parses as valid YAML with name == "web-research".
  </behavior>
  <action>
Extend `build_hermes_cmd` in parliament/agent_factory.py with an optional keyword-only parameter
`toolsets: list[str] | None = None`. When provided and non-empty:
- Validate each toolset id (non-empty, no whitespace, no "/") — raise ValueError otherwise, mirroring
  the existing skill-id validation style.
- Append `-t <comma-joined toolsets>` to the returned argv.
When None/empty, return the current argv unchanged (preserve backward compatibility — existing
session.py callers must not break).

Then write tests/test_web_search_agent.py covering the behaviors above. Use pyyaml (already a dep)
to parse the web-research frontmatter. Mark any test that actually spawns `hermes` (live web call)
with `@pytest.mark.slow` and guard it so it is collected but skipped by default — the default unit
tests must be pure argv/YAML assertions with no network or subprocess.

Do NOT wire web-research into the Marszałek delegate_task flow in this plan — that is downstream
orchestration work. This plan only makes the capability available and testable.
  </action>
  <verify>
    <automated>python -m pytest tests/test_web_search_agent.py -x -q -m "not slow"</automated>
  </verify>
  <done>All non-slow tests in tests/test_web_search_agent.py pass; build_hermes_cmd emits `-t web` when toolsets=["web"] and is unchanged when toolsets is omitted.</done>
</task>

</tasks>

<verification>
- `python -c "import ddgs"` succeeds (keyless backend installed).
- `~/.hermes/config.yaml` has `web.search_backend: ddgs`.
- `npx skills-ref@0.1.5 validate skills/web-research` exits 0.
- `python -m pytest tests/test_web_search_agent.py -m "not slow"` passes.
- Optional manual/live check: `hermes chat -s web-research -q "Polish minimum wage 2026" -t web -Q --accept-hooks --yolo` returns results with source URLs.
</verification>

<success_criteria>
- The Hermes CLI is confirmed present and configured with a keyless (`ddgs`) web search backend — no new secret required for a clean clone.
- A spec-valid `web-research` agent skill exists that instructs the agent to use `web_search`/`web_extract` and cite source URLs.
- `agent_factory.build_hermes_cmd` can attach the `web` toolset (`-t web`) while remaining backward compatible with all existing callers.
- A unit test proves the toolset wiring and skill validity without network access.
</success_criteria>

<output>
After completion, create `.planning/quick/260528-ea6-install-agent-cli-and-build-web-search-a/260528-ea6-SUMMARY.md`
</output>
